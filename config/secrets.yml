# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rake secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

development:
  admin_name: <%= ENV["ADMIN_NAME"] %>
  admin_email: <%= ENV["ADMIN_EMAIL"] %>
  admin_password: <%= ENV["ADMIN_PASSWORD"] %>
  domain_name: <%= ENV["DOMAIN_NAME"] %>
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
  paypal_login: <%= ENV["PAYPAL_LOGIN"] %>
  paypal_password: <%= ENV["PAYPAL_PASSWORD"] %>
  paypal_signature: <%= ENV["PAYPAL_SIGNATURE"] %>
  email_provider_username: <%= ENV["EMAIL_PROVIDER_USERNAME"] %>
  email_provider_password: <%= ENV["EMAIL_PROVIDER_PASSWORD"] %>
test:
  domain_name: example.com 
  secret_key_base: 69ee0da365004eef4858c57c53da10127cbad034484fc167896548b8d4a1969e985e7589b17f57a549c81a2e2f7436fa70beeba48ef3632e5eb0e8df2502a03f

# Do not keep production secrets in the repository,
# instead read values from the environment.
production:
  admin_name: <%= ENV["ADMIN_NAME"] %>
  admin_email: <%= ENV["ADMIN_EMAIL"] %>
  admin_password: <%= ENV["ADMIN_PASSWORD"] %>
  domain_name: <%= ENV["DOMAIN_NAME"] %>
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
  paypal_login: <%= ENV["PAYPAL_LOGIN"] %>
  paypal_password: <%= ENV["PAYPAL_PASSWORD"] %>
  paypal_signature: <%= ENV["PAYPAL_SIGNATURE"] %>
  email_provider_username: <%= ENV["EMAIL_PROVIDER_USERNAME"] %>
  email_provider_password: <%= ENV["EMAIL_PROVIDER_PASSWORD"] %>
